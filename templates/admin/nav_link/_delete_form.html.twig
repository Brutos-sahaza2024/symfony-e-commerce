<form method="post" action="{{ path('app_nav_link_delete', {'id': nav_link.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nav_link.id) }}">
    <button class="btn">Delete</button>
</form>
