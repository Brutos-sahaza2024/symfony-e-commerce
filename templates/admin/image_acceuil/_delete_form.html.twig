    <div class="container">
        <form method="post" action="{{ path('app_image_acceuil_delete', {'id': image_acceuil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_acceuil.id) }}">
            <button class="btn btn-danger">
                <i class="fas fa-trash-alt"></i> Delete
            </button>
        </form>
    </div>